version: "1"
name: hello_world
enabled: true
tags:
- training

triggers:
  - type: manual
    inputs:
      - name: username
        type: string
        required: true
        description: "The name of the user to greet"

steps:
  - name: print_greeting
    type: console
    with:
      message: "Hello, {{ inputs.username }}!"