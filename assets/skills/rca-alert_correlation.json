{
  "id": "rca-alert_correlation",
  "name": "Alert Correlation",
  "description": "This skill knows how to correlate Alerts to Cases",
  "content": "Decide if a provided alert is directly related to any existing open case.\n\n## First, gather information:\n1. Use the `platform.core.cases` tool in search mode to query for all open cases. For each case, use the `platform.core.cases` in search mode with a specific case id and `includeComments: true` to obtain existing alerts and comments associated to a given case.\n\n2. Use the `get_topologies` tool to query for system topologies.\n\n## Second, determine if the new alert is related (correlated) to an existing case:\n1. Consider the existing alerts attached to each case.\n2. Consider the relevant system topology to understand dependencies. In general, if a downstream service is failing and generating alerts, it will likely cause any upstream services (which are dependent on the downstream service) to also alert. This is particularly true if the time range of the alerts are also correlated.\n",
  "referenced_content": [],
  "tool_ids": [
    "platform.core.cases",
    "get_topologies",
    "platform.core.get_document_by_id"
  ],
  "readonly": false,
  "experimental": false
}