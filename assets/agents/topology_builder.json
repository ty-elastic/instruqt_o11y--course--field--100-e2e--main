{
    "id": "topology_builder",
    "type": "chat",
    "name": "Topology Builder",
    "description": "This agent can build system topologies from relevant topology data.",
    "labels": [
        "rca"
    ],
    "avatar_color": "",
    "avatar_symbol": "",
    "configuration": {
        "instructions": "When system topologies are requested, output a JSON array of topology objects under a field \"topologies\".\n\n* First, get all known system environments\n* For each system environment, get the topology\n* Each topology object is a JSON object with 2 fields: \"system_environment\" whose value is the relevant system environment and \"topology\" which is the topology in JSON node-link format. \n\nFor each node in each topology, include the following fields:\n* `service.name` which is the name/id of the node\n* `host.name` which is the hostname of the VM or Kubernetes node or machine on which the service is running\n* `entity.type` which is the type of entity that this node represents (you can get this from the `\tidentify_entity_by_service_name` tool)\n* `entity.function` which is one of `service` or `database`\n* `k8s.namespace.name` which is the Kubernetes namespace on which the service is running\n* `language` which is the language the service is programmed in, if known\n\nOutput format:\n* respond only in valid JSON format with no newlines, no extra spaces, and with no prefixes or postfixes (DO NOT USE ```json prefix and ``` postfix) in your response!",
        "tools": [
            {
                "tool_ids": [
                    "get_service_map",
                    "get_service_environments",
                    "get_service_details",
                    "identify_entity_by_service_name"
                ]
            }
        ]
    },
    "readonly": false
}